# 로그스토리 구조 기록

## 실행 구조
React `Home` 페이지는 게임 본체 `/logstory.html`을 iframe으로 감싸는 프레임 역할을 한다. 실제 게임 화면, 전투 상태, 저장, 지역, 직업, 장비, 전투 로직은 `client/public/logstory.html` 안에 남아 있다.

## 이번 변경 경계
서버와 React 호스트 구조는 변경하지 않는다. `client/public/logstory.html`의 전투 DOM·CSS·표시 연결·스프라이트 URL·모션 함수만 변경한다. 모든 전투 계산과 저장 키 구조는 그대로 유지한다.

## 도트 자산 연결
`startBattle()`가 몬스터 이름과 보스 여부를 바탕으로 chibi 자산 URL을 선택한다. 플레이어는 일반시민의 둔기 보유 스프라이트를 사용하고, 들쥐떼·변이견·일반·갑주형·보스 몬스터는 서로 다른 매핑을 사용한다. 전투 HUD는 현재 지역명과 `[처치 수/10]`을 표시한다.
