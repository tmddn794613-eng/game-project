# 로그스토리 도트 전투 전환 계획

## 목표
기존 단일 HTML RPG의 전투·저장·성장 로직은 보존하고, 전투 화면의 이모지 표현을 2D 도트풍 스프라이트로 교체한다. 공격과 피격이 사용자에게 명확하게 보이는 모션과 자원 피드백을 제공한다.

## 위험 요소와 대응

| 위험 요소 | 대응 |
|---|---|
| CSS 애니메이션이 전투 시점에 보이지 않음 | CSS 클래스만의 의존을 줄이고 `Element.animate()` 직접 호출로 공격·반동을 재생한다. |
| 도트 이미지가 흐릿하게 보임 | 투명 PNG를 92px로 표시하고 `image-rendering: pixelated`를 적용한다. |
| 기존 전투 로직 손상 | `startBattle`, `attack`, `skillAttack`, `enemyAction` 내부의 표시 연결만 변경하고 피해·보상 계산은 유지한다. |
| 모바일 화면에서 스프라이트가 겹침 | 전투 영역 안에 좌우 고정 배치하고 돌진 거리를 제한한다. |

## 검증 기준

전투에 진입하면 플레이어와 몬스터가 도트 이미지로 표시되어야 한다. 플레이어 공격 시 플레이어가 전진하고 몬스터가 반동해야 하며, 몬스터 공격 시 반대 동작이 보여야 한다. HP 피해 및 MP 소비 시 해당 자원 막대가 빨간색으로 깜빡이고 중앙 수치와 폭이 함께 갱신되어야 한다. JavaScript 문법 검사와 `pnpm check`, `pnpm build`를 통과해야 한다.
