# 로그스토리 작업 메모

기존 공격 모션은 CSS 클래스 추가 방식이었지만 테스트에서 사용자가 체감할 수 없었다. 이번 보완에서는 `triggerMotion()`이 `Element.animate()`로 직접 transform keyframe을 재생하도록 변경했다. 공격은 약 620ms 동안 돌진·복귀하고 피격은 약 480ms 동안 반동·복귀한다. 피격 대상에는 이미지 밝기 깜빡임을 함께 적용한다.

전투 화면은 현재 이모지 대신 작고 아기자기한 16비트 JRPG풍 chibi 투명 PNG 도트 스프라이트를 사용한다. 일반시민은 소총이 아닌 낡은 둔기를 들며, 들쥐떼는 변이견과 구별되는 작은 설치류 무리로 매핑한다. 이미지가 지연 로드되더라도 DOM 위치와 모션은 유지되어야 하므로 모션은 부모 `.fighter`에 적용한다.

던전 진입 중 전투 HUD 왼쪽 위에는 지역명과 `[처치 수/10]`을 표시한다. 일반 몬스터 10마리 처치 시 다음 몬스터 자동 진입 대신 `bossReady` 팝업을 띄워 보스 사냥 또는 던전 복귀를 선택한다. 도주는 확인 팝업 없이 한 번 예약하면 성공할 때까지 ATB가 찰 때마다 자동 재시도한다. 기존 저장 데이터의 `노전직`은 불러올 때 `일반시민`으로 호환 변환한다.
